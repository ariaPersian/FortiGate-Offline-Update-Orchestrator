Implementation: `fgops-agent` now enters through `fgops.entrypoint`, which installs the strict prompt matcher before importing the operational CLI. This keeps the change narrowly scoped while retaining the existing FortiGate session and command allowlists.
