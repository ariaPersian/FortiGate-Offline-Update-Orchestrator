Operator action after upgrade: keep the scheduled task disabled, verify the failed manifest still has unchanged AV versions, reset only that archive to PREPARED, run one foreground `cycle`, inspect the final package results, and re-enable the task only after success.
