# v0.5.2 test intent

Regression coverage asserts that `##` and longer hash-only TFTP progress lines are not FortiGate CLI prompts, while a normal prompt such as `SITEC-FW-02 (global) #` remains detectable.
