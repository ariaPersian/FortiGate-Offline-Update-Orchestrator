Root cause: the original regular expression matched any short line ending in `#` or `$`. FortiGate emits transfer progress as hash-only lines, so those lines were mistaken for prompts. The post-restore command was sent before TFTP completion and the client then closed the session.
